#!/bin/bash

# 切换用户
CURRENT_USER=$(getent passwd 1000 | cut -d: -f1)
if [ "$EUID" -eq 0 ]; then
    exec runuser -u "${CURRENT_USER}" -- "$0" "$@"
fi

XDG_RUNTIME_DIR=/run/user/1000 systemctl --user start pulseaudio.service

exit 0